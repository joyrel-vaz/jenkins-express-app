{
  "dependencies": {
    "express": "^4.18.1",
    "netlify-lambda": "^2.0.15",
    "serverless-http": "^3.0.2"
  },
  "name": "express",
  "version": "1.0.0",
  "description": "testing express for jenkins",
  "main": "app.js",
  "scripts": {
    "test": "mocha test --reporter mocha-junit-reporter",
    "start": "npx netlify-lambda serve src",
    "build": "npx netlify-lambda build src"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.0.2"
  }
}
